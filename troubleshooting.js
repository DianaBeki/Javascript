 function troubleshooting () {
  
  const a = +2;
  const b = +3;

  let result = "a" + "b";

  return result;
 }

 troubleshooting();

 //The unary + operator is used to convert a variable to a number.
